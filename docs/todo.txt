GENERAL:
    - Let the admin user input json data to create multiple objects.
    - Implement the operations tab in the admin page.
        - Create "Operation" model.
        - Update views to register operations made.
    - Add reservations page.
    - Add pagination to object listing in admin page.
    - Add filtering and sorting to Showtime listing in admin page.

BACKEND:

FRONTEND:
    - Fix movie display at home page.
    - Fix display of auth modal.
    - Fix display of seat selector.
    - Add loading indicators.
    - Add notifications:
        - Error message on wrong credentials at deleting an object.
        - Success message when creating, updating or deleting an object, at the operations tab.
        