TODO: Main
- let the admin user input json data to create multiple objects

- Change showtime form's select movie field for a search bar.
    - Move search bar from navbar to a separate component and use it in showtime form.

- Display an alert in the operations tab when updating or deleting an object.

- Create a new "Operation" model to store all operations.
    - Update views to create operations.

- Fix reservation status auto updating.

- Add loading indicators to:
    - Components that rely on user authentication status.
    - Checkout page.

- Fix multiple requesting.

- Add user and mail checking when registering.

Big and complex:
- 4. Include email system (When creation an account and making reservations).
- 5. Add admin page.
- 6. Add payment system.